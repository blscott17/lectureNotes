/*
Create and initialize three different variables, each with a different data type. console.log() each one. Also write one function to console.log()s 'Hello, World!'. When you're done, submit a link to the pen.
*/

// Constant String variable
const name = "Bonnie Scott";
console.log(name);

// Boolean value variable
var hasChildren = true;

if(hasChildren === true) {   
    console.log("She is a Momma Bear!"); 
} 

// Numeric variable value
let maxNumberScreenHours = 4;
    console.log("What is the maximum number of screen hours you allow your children per day? " + maxNumberScreenHours);

//function to say Hello World!
    function hello() {
    console.log("Hello World!");}